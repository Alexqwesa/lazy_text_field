# lazy_text_field_example

Visual test app for `LazyTextField`.

```sh
cd packages/lazy_text_field/example
flutter run -d chrome
```

Use the toolbar to compare edit-only, always-visible, and hidden decoration
chrome; bounded height; prefix/suffix icon width reservation; and lazy active
controller allocation in a large list.
